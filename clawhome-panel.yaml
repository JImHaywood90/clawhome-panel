# ClawHome Panel — ESPHome entry point
# Copy this file into your ESPHome config directory (/config/esphome/)
# Also copy secrets.yaml to /config/esphome/secrets.yaml (or add the keys to your existing one)
# Everything else pulls from GitHub automatically.

packages:
  clawhome_panel:
    url: https://github.com/JImHaywood90/clawhome-panel
    ref: 2026.1.4
    file: src/main.yaml
    refresh: 0s
